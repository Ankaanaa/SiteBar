const iconMenu=document.querySelector(".menu__icon");if(iconMenu){const a=document.querySelector(".menu__body");iconMenu.addEventListener("click",e=>{iconMenu.classList.toggle("_active"),a.classList.toggle("_active"),document.body.classList.toggle("_lock")})}